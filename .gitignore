# Credenciales reales (las escribe install.php; no van en el ZIP)
/config/config.local.php
/config/instalado.lock

# Carpeta de sesiones si el hosting la usa dentro del proyecto
/tmp_sess/

# Documentos subidos (ver uploads/.gitignore)
/uploads/*
!/uploads/.htaccess
!/uploads/.gitignore
